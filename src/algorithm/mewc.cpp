#include "mewc.hpp"
#include "../common.hpp"

Clique exactMEWC(Graph g)
{
    UNUSED(g);
    // TODO : implement the exact algorithm
    return Clique();
}

Clique constructiveMEWC(Graph g)
{
    UNUSED(g);
    // TODO : implement the constructive algorithm
    return Clique();
}

Clique localSearchMEWC(Graph g)
{
    UNUSED(g);
    // TODO : implement the local search algorithm
    return Clique();
}

Clique graspMEWC(Graph g)
{
    UNUSED(g);
    // TODO : implement the grasp algorithm
    return Clique();
}